# Workspace-local ignores. Most things are already excluded by the root
# .gitignore; these are explicit safeguards in case this directory is
# ever consumed standalone.

node_modules
dist
.vite
*.log

# Local TypeScript build info
*.tsbuildinfo

# Bundle analysis output (generated by pnpm analyze)
stats.html
